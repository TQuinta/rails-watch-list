<h2>My bookmarks for <%= List.find(params[:list_id]).name %></h2>
<ul>
  <% @bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %></li>
  <% end %>
</ul>

<%= link_to "all lists", lists_path %>
